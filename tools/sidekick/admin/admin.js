export async function decorate(a, b, c, d, e, f) {
  // console.log(`A ${JSON.stringify(a)}`);
  console.log(`B ${JSON.stringify(b)}`);
  console.log(`C ${c}`);
  console.log(`D ${JSON.stringify(d)}`);
  console.log(`E ${JSON.stringify(e)}`);
  console.log(`F ${JSON.stringify(f)}`);
}

export default {
  title: 'Admin',
  searchEnabled: true,
};
